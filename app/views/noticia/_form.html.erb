<%= form_with(model: noticium, local: true) do |form| %>
  <% if noticium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(noticium.errors.count, "error") %> prohibited this noticium from being saved:</h2>

      <ul>
      <% noticium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo, id: :noticium_titulo %>
  </div>

  <div class="field">
    <%= form.label :bajada %>
    <%= form.text_field :bajada, id: :noticium_bajada %>
  </div>

  <div class="field">
    <%= form.label :cuerpo %>
    <%= form.text_field :cuerpo, id: :noticium_cuerpo %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link, id: :noticium_link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
